---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { recentProjects } from '../../data/projects';
---

<BaseLayout title="Projects" description="Project case studies by Arhum Savera">
  <section class="hero">
    <h1>Projects</h1>
    <p>Work I've shipped — production AI infrastructure, edge CV, and the agent tooling I've been building in 2026.</p>
  </section>

  <section class="section">
    <div class="grid-2">
      {recentProjects.map((project) => (
        <a class="card" href={`/post/${project.slug}/`}>
          <h3>{project.title}</h3>
          <p>{project.summary}</p>
          <div class="badge-row">
            <span class="badge">{new Date(project.date).toLocaleDateString('en-US', { month: 'short', year: 'numeric' })}</span>
          </div>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>
